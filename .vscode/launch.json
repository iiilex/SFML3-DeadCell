{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Dead_Cell (Debug)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/Dead_Cell",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": "${defaultBuildTask}"
        }
    ]
}